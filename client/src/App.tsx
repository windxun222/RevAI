import { useState, useCallback } from 'react'; import { Home } from './pages/Home'; import { Analysis } from './pages/Analysis'; import { Finding, AnalysisStats, AnalysisSummary } from './types';
export type View = 'home'|'analysis';
function App() { const [view,setView]=useState<View>('home'); const [aid,setAid]=useState<string|null>(null); const [prUrl,setPrUrl]=useState(''); const [status,setStatus]=useState(''); const [progress,setProgress]=useState(0); const [findings,setFindings]=useState<Finding[]>([]); const [summary,setSummary]=useState<AnalysisSummary|undefined>(undefined); const [stats,setStats]=useState<AnalysisStats|null>(null); const [pr,setPr]=useState<any>(null); const [l1c,setL1c]=useState(false); const [l2c,setL2c]=useState(false);
  const handleStart=useCallback((id:string,url:string)=>{ setAid(id); setPrUrl(url); setView('analysis'); setFindings([]); setSummary(undefined); setStats(null); setProgress(0); },[]);
  const handleBack=useCallback(()=>{ setView('home'); setAid(null); setStatus(''); setProgress(0); setFindings([]); setSummary(undefined); setStats(null); setPr(null); },[]);
  return (<div className="min-h-screen"><header className="border-b border-gray-800 bg-gray-900/50 backdrop-blur sticky top-0 z-50"><div className="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between"><div className="flex items-center gap-3"><div className="w-8 h-8 rounded-lg bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center text-sm font-bold text-white">R</div><div><h1 className="text-sm font-semibold tracking-tight">RevAI</h1><p className="text-[10px] text-gray-500">AI PR Review Assistant</p></div></div>{view==='analysis'&&<button onClick={handleBack} className="text-xs text-gray-400 hover:text-white">New Analysis</button>}</div></header><main className="max-w-7xl mx-auto">{view==='home'&&<Home onAnalysisStart={handleStart}/>}{view==='analysis'&&aid&&<Analysis analysisId={aid} prUrl={prUrl} status={status} setStatus={setStatus} progress={progress} setProgress={setProgress} findings={findings} setFindings={setFindings} summary={summary} setSummary={setSummary} stats={stats} setStats={setStats} pr={pr} setPr={setPr} l1Complete={l1c} setL1Complete={setL1c} l2Complete={l2c} setL2Complete={setL2c} onBack={handleBack}/>}</main></div>); }
export default App;
